let numeros = [6,7,8,9,4,2]
let cantidadNumeros = numeros.length

.for (let i = 0; i < cantidadNumeros; i++) {
    numeros[i] = numeros[i]*2 //si queremos que se multiplique
    numeros[i] = numeros[i]+1 //si queremos que se sume
    
}
console.log(numeros)

//reemplazar 0 por 100
let nums = [20,0,9,0]
for (let i = 0; i < array.length; i++) {
   if(numeros[i] == 0){
    numeros[i] = 100
   }
}
